# ------------------------------------------------------------------
#  Core tooling
# ------------------------------------------------------------------
papermill
ipykernel

# ------------------------------------------------------------------
#  Azure ML SDK (v1) + MLflow logger
# ------------------------------------------------------------------
azureml-sdk
azureml-mlflow
azureml-dataset-runtime      # dataset helpers used in utils_nlp

# ------------------------------------------------------------------
#  Deep‑learning / NLP stack  (CPU wheels – works on cpu‑cluster)
# ------------------------------------------------------------------
torch
torchvision
torchaudio
--extra-index-url https://download.pytorch.org/whl/cpu

transformers
datasets
sentencepiece

# ------------------------------------------------------------------
#  utils_nlp dependencies
# ------------------------------------------------------------------
nltk
spacy
dask[dataframe]
pyarrow           # parquet I/O for dask / datasets
fastparquet
scikit-learn
pandas
numpy          # ✅ works with AzureML SDK v1
tqdm
matplotlib


# ------------------------------------------------------------------
#  Optional quality‑of‑life (harmless if notebook doesn’t use them)
# ------------------------------------------------------------------
seaborn

# utils_nlp extra
googledrivedownloader    # ← correct package name
# ------------------------------------------------------------------
#  EXTRA Azure ML sub‑packages used in the BERT pipeline
# ------------------------------------------------------------------
azureml-pipeline-core
azureml-pipeline-steps
azureml-train-core
azureml-widgets     # for RunDetails() in the notebook UI

# ------------------------------------------------------------------
#  Misc utilities still missing
# ------------------------------------------------------------------
jsonlines            # needed by utils_nlp.models.transformers.datasets
