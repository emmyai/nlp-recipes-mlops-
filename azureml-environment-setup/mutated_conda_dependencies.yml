name: bert-environ_1
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - numpy
  - pandas
  - scikit-learn
  - pip:
      - azureml-core
      - azureml-defaults
      - torch
      - transformers
      - datasets
      - tokenizers
      - accelerate
      - tqdm
      - spacy
      - dask  # Latest Python 3.9 compatible version


